<!-- SUBMIT -->
<div class="submit-card">
    <button type="submit" class="btn-simpan">
        <i class='bx bx-save'></i> {{ $buttonText }}
    </button>
    <p class="btn-subtext">{{ $subText }}</p>
    <a href="{{ $cancelUrl }}" class="btn-batal">
        <i class='bx bx-x'></i> 
        Batal
    </a>
</div>